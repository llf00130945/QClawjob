# QClaw 智能体前端项目文档

> 运行在本地服务器上，通过 Express 反向代理桥接 QClaw Gateway，为多个智能体提供统一的 Web 对话界面。

## 目录结构

```
F:\Qclawjob\
├── start.bat              # 一键启动脚本
├── backend\
│   ├── server.js          # Express 后端代理服务（端口 3000）
│   ├── package.json       # Node.js 项目配置
│   └── node_modules\      # 依赖（express、multer）
├── public\
│   └── index.html         # 前端单页应用（深色主题 Web UI）
├── uploads\               # 文件上传的临时目录（POST 处理后即时清理）
└── 项目总结.md            # 本文件
```

## 核心架构

```
┌─────────────────────────────────────────────────────────┐
│                    浏览器（用户）                          │
│            http://localhost:3000                          │
└─────────────────────┬───────────────────────────────────┘
                      │ SSE (text/event-stream)
                      ▼
┌─────────────────────────────────────────────────────────┐
│              Express 反向代理（Node.js）                    │
│              端口 3000 / 0.0.0.0                          │
│  ┌───────────┬───────────┬────────────────────┐          │
│  │ /api/chat │/api/chat- │  /api/agents        │          │
│  │ (SSE转发) │with-file  │  /api/status        │          │
│  └─────┬─────┴───┬───────┴────────────────────┘          │
│        │         │                                         │
│        └────┬────┘                                         │
└─────────────┼───────────────────────────────────────────┘
              │ POST /v1/chat/completions (stream: true)
              │ Authorization: Bearer <token>
              ▼
┌─────────────────────────────────────────────────────────┐
│            QClaw Gateway (内置 QClaw.exe)                   │
│                端口 62433                                  │
│  ┌─────────────────────────────────────┐                  │
│  │ Agent Router                         │                  │
│  │  openclaw/agent-168feb0f  🏗         │                  │
│  │  openclaw/agent-2e63987e  🏆         │                  │
│  └──────────┬──────────────────────────┘                  │
│             │ modelroute + 各 Agent 独有 SOUL.md 人设       │
│             ▼                                              │
│        DeepSeek / 其他推理模型                               │
└─────────────────────────────────────────────────────────┘
```

### 请求流向

1. 用户在浏览器输入消息 → 前端 JS 构造请求
2. 前端通过 `fetch` 发送 POST 到后端 `/api/chat` 或 `/api/chat-with-file`
3. 后端组装 `messages`（System Prompt + 用户消息 + 文件上下文），转发到 Gateway 的 `/v1/chat/completions`
4. Gateway 根据 `model` 参数（`openclaw/agent-xxx`）路由到对应智能体
5. Gateway 返回 SSE 流式响应 → 后端逐块解析 → 以 SSE 格式写给前端
6. 前端逐块更新气泡，完成对话

### 两个核心端点

| 端点 | 方法 | 用途 | 说明 |
|------|------|------|------|
| `POST /api/chat` | 纯文本对话 | `{ messages, agentId }` → SSE 流式回复 |
| `POST /api/chat-with-file` | 文件+文本对话 | `multipart/form-data`，含 `message`, `agentId`, `files[]` |
| `GET /api/status` | 状态检查 | 返回 Gateway 连接状态 |
| `GET /api/agents` | 智能体列表 | 返回所有注册智能体信息+System Prompt |

## 关键技术点

### 1. SSE 流式代理

后端使用 **`fetch` API** 连接 Gateway 的 SSE 端点，原因如下：

- **`fetch + response.body.getReader()`** 是 Node.js 原生支持的标准流式读取方式，能够正确接收 `text/event-stream` 分块数据
- 早期尝试的 `http.request` 方案在收到分块数据时报 `socket hang up`，因为 Node.js 原生 `http` 模块处理 `Transfer-Encoding: chunked` + SSE 时有兼容问题
- 后端收到 Gateway 的 SSE 数据后，**按行解析** SSE 事件（`data:` 前缀），提取 `choices[0].delta.content`，再以 SSE 格式转发给前端

### 2. 客户端消息合并

前端在发送时，若 `attachedFiles` 中有文件，则：

1. 将文件通过 `FormData` + `fetch` 上传到 `/api/chat-with-file`
2. 后端用 **multer** 接收 multipart，提取文件文本内容
3. 将文件文本拼接为上下文，添加 System Prompt，构造完整的 `messages` 数组
4. 发送到 Gateway，完成对话

### 3. 智能体切换

- 后端 `/api/agents` 返回所有注册智能体的 ID、名称、头像、System Prompt
- 前端加载后自动获取列表，渲染切换下拉菜单
- 切换智能体时：清空聊天区 → 重置 `messageHistory` → 更新 Header/Welcome 信息 → 更新建议问题按钮列表

### 4. 文件上传

- 支持多达 10 个文件（每个≤10MB）
- 支持的格式：txt / md / json / xml / log / csv / pdf / doc / docx / xls / xlsx / png / jpg / jpeg / gif / webp / bmp
- 文本类文件直接读取 UTF-8 文本；Office/PDF 尝试原始文本提取
- 图片文件仅返回 base64 长度占位符（不做 OCR）
- 上传的文件在处理后**立即删除**，不残留

## 已配置智能体

| 智能体 ID | 名称 | 头像 | 人设来源 | 说明 |
|-----------|------|------|----------|------|
| `agent-168feb0f` | 企业工程管理总监 | 🏗 | `workspace-agent-168feb0f/SOUL.md` | 30年施工企业管理经验，项目进度/安全/质量/成本 |
| `agent-2e63987e` | 工程创优专家 | 🏆 | `workspace-agent-2e63987e/SOUL.md` | 鲁班奖专家，创优方案策划，优质工程案例库 |

> **注意**：添加新智能体需要同时在 backend `server.js` 的 `AGENTS` 对象中注册 agentId，并在 `public/index.html` 的 `getAgentDesc()` 和 `renderSuggestions()` 中添加对应的描述和建议问题。

## 关键配置

配置文件：`F:\Qclawjob\backend\server.js`

```javascript
const GATEWAY_HOST = '127.0.0.1';      // QClaw Gateway 地址
const GATEWAY_PORT = 62433;             // QClaw Gateway 端口（QClaw.exe 内置）
const AUTH_TOKEN = 'ce90499e...';       // Gateway 认证 Token

const AGENTS = {
    'agent-168feb0f': { name: '企业工程管理总监', avatar: '🏗' },
    'agent-2e63987e': { name: '工程创优专家',    avatar: '🏆' }
};
```

## 启动与使用

### 手动启动

```bash
cd F:\Qclawjob\backend
node server.js
```

### 一键启动

双击 `F:\Qclawjob\start.bat`

启动后浏览器访问：**http://localhost:3000**

### 启动日志

```
========================================
     QClaw Agent 前端服务已启动
========================================

  📍 http://localhost:3000
  🏗 企业工程管理总监 (agent-168feb0f)
  🏆 工程创优专家 (agent-2e63987e)
  🔗 Gateway @ http://127.0.0.1:62433
```

## 版本历史

| 日期 | 功能 | 说明 |
|------|------|------|
| 2026-06-12 | 初始搭建 | 创建 Express 反向代理，Gateway SSE 流式连接 |
| 2026-06-12 | SSE 流式修复 | 从 `http.request` 改为 `fetch + getReader()`，解决 socket hang up |
| 2026-06-12 | 文件上传 | 支持 multipart 上传、文本提取、即时清理 |
| 2026-06-12 | 重复声明 Bug 修复 | 修复 `let attachedFiles` 重复声明导致的 JS 执行中断 |
| 2026-06-12 | 网关端口修正 | 修正 Gateway 端口为 62433（实际运行端口） |
| 2026-06-13 | 多智能体切换 | 支持 "企业工程管理总监" + "工程创优专家" 切换，System Prompt 联动 |
