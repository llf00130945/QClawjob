# 基于 QClaw 二次开发的局域网智能体网页调用平台

> 项目位置：`F:\Qclawjob`  
> 创建时间：2026-06-12 ~ 2026-06-13  
> 技术栈：Node.js + Express + 原生 HTML/CSS/JS

---

## 一、项目原理

### 1.1 核心架构

本项目是一个 **Express 反向代理服务**，架设在浏览器用户与 QClaw Gateway 之间，将 QClaw 内置的 AI 智能体能力通过网页界面暴露给局域网内用户。

```
┌─────────────────────────────────────────────────────────────┐
│                    浏览器（用户）                            │
│              http://localhost:3000                            │
└──────────────────────┬──────────────────────────────────────┘
                       │ SSE (text/event-stream)
                       ▼
┌─────────────────────────────────────────────────────────────┐
│              Express 反向代理（Node.js）                       │
│              端口 3000 / 监听 0.0.0.0（局域网可访问）           │
│  ┌───────────┬──────────────┬────────────────────────┐        │
│  │ /api/chat │/api/chat-    │  /api/agents            │        │
│  │ (SSE转发) │with-file     │  /api/status            │        │
│  └─────┬─────┴──────┬───────┴────────────────────────┘        │
│        │            │                                         │
│        └────────────┴────────────┐                            │
└──────────────────────────────────┼──────────────────────────┘
                                   │ POST /v1/chat/completions (stream: true)
                                   │ Authorization: Bearer <token>
                                   ▼
┌─────────────────────────────────────────────────────────────┐
│            QClaw Gateway（内置 QClaw.exe）                    │
│                端口动态分配（50233 / 62433 等）                 │
│  ┌─────────────────────────────────────┐                      │
│  │ Agent Router                       │                      │
│  │  openclaw/agent-168feb0f  🏗        │                      │
│  │  openclaw/agent-2e63987e  🏆        │                      │
│  └──────────┬──────────────────────────┘                      │
│             │ modelroute + 各 Agent 独有 SOUL.md 人设          │
│             ▼                                                │
│        DeepSeek / 其他推理模型                                │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 请求流向

1. 用户在浏览器输入消息 → 前端 JS 构造请求
2. 前端通过 `fetch` 发送 POST 到后端 `/api/chat` 或 `/api/chat-with-file`
3. 后端组装 `messages`（System Prompt + 用户消息 + 文件上下文），转发到 Gateway 的 `/v1/chat/completions`
4. Gateway 根据 `model` 参数（`openclaw/agent-xxx`）路由到对应智能体
5. Gateway 返回 SSE 流式响应 → 后端逐块解析 → 以 SSE 格式写给前端
6. 前端逐块更新气泡，完成对话

### 1.3 关键技术原理

| 技术点 | 实现方式 | 原因 |
|--------|----------|------|
| **SSE 流式代理** | `fetch` + `response.body.getReader()` | Node.js 原生支持标准流式读取，解决 `http.request` 的 `socket hang up` 问题 |
| **Gateway 端口检测** | 三层检测：配置文件 → status 命令 → netstat 兜底 | QClaw Gateway 端口每次启动动态分配，必须自动检测 |
| **智能体切换** | 从 `workspace-agent-xxx/SOUL.md` 动态读取 System Prompt | 切换时自动加载对应人设 |
| **文件上传** | `multer` 接收 multipart，提取文本后拼接为上下文 | 将文件内容作为 System Prompt 的一部分送入对话 |

---

## 二、项目框架

### 2.1 目录结构

```
F:\Qclawjob\
├── start.bat                  # 一键启动脚本（批处理，可能受安全策略限制）
├── start-server.ps1           # PowerShell 启动脚本（推荐）
├── launcher.js                # Node.js 启动器（跨平台）
├── README.md                  # 项目说明
├── backend\
│   ├── server.js              # Express 后端代理服务（核心）
│   ├── package.json           # Node.js 依赖配置
│   ├── server.log             # 运行日志
│   └── node_modules\          # express、multer 等依赖
├── public\
│   └── index.html             # 前端单页应用（深色主题 Web UI）
└── uploads\                   # 文件上传临时目录（处理后即时清理）
```

### 2.2 后端 API 端点

| 端点 | 方法 | 用途 | 说明 |
|------|------|------|------|
| `POST /api/chat` | 纯文本对话 | `{ messages, agentId }` → SSE 流式回复 |
| `POST /api/chat-with-file` | 文件+文本对话 | `multipart/form-data`，含 `message`, `agentId`, `files[]` |
| `GET /api/status` | 状态检查 | 返回 Gateway 连接状态 |
| `GET /api/agents` | 智能体列表 | 返回所有注册智能体信息 + System Prompt |

### 2.3 前端界面结构

- **Header**：智能体切换下拉菜单（🏗 企业工程管理总监 / 🏆 工程创优专家）
- **聊天区**：消息气泡（用户蓝色右对齐 / AI 灰色左对齐）
- **输入区**：文本输入框 + 📎 文件上传按钮 + 发送按钮
- **欢迎界面**：默认显示，首次对话后隐藏
- **快捷建议**：根据当前智能体动态显示建议问题

---

## 三、实现目的

### 3.1 核心目标

| 目标 | 说明 |
|------|------|
| **网页化调用** | 将 QClaw 的 AI 智能体能力通过浏览器网页暴露，无需安装客户端 |
| **局域网共享** | 监听 `0.0.0.0:3000`，同一局域网内多台设备可访问 |
| **多智能体切换** | 在一个界面中切换不同专业智能体（工程管理 / 创优专家） |
| **文件上传对话** | 支持上传 PDF/Word/Excel/图片等文件，提取内容后送入对话 |
| **流式响应** | SSE 实时显示 AI 回复，逐字输出，提升用户体验 |

### 3.2 解决的问题

- **QClaw 无原生 Web 界面**：只能通过 API 或客户端调用，本项目提供完整 Web UI
- **Gateway 端口动态变化**：每次启动端口不同，通过三层自动检测解决
- **多智能体管理不便**：QClaw 中切换智能体需要重新配置，本项目统一界面管理
- **文件输入困难**：QClaw 原生不支持文件上传，本项目通过 multer 实现

---

## 四、项目优点

### 4.1 技术优点

| 优点 | 说明 |
|------|------|
| **零构建工具** | 前端纯原生 HTML/CSS/JS，无需 React/Vue/Webpack，部署简单 |
| **SSE 流式传输** | 实时显示 AI 回复，逐字输出，体验接近 ChatGPT |
| **自动端口检测** | 三层检测（配置文件 → status 命令 → netstat），自动适应 Gateway 端口变化 |
| **动态智能体加载** | 从 `SOUL.md` 自动读取 System Prompt，新增智能体无需修改代码 |
| **局域网共享** | 监听 `0.0.0.0`，同一网络内多设备可同时使用 |
| **即时文件清理** | 上传文件处理后立即删除，不残留敏感数据 |

### 4.2 使用优点

| 优点 | 说明 |
|------|------|
| **一键启动** | PowerShell 脚本支持 start/stop/restart/status |
| **深色主题** | 专业工程管理风格，长时间使用不疲劳 |
| **多文件上传** | 支持最多 10 个文件同时上传 |
| **智能体记忆** | 切换智能体时保留各自的历史记录 |
| **快捷建议** | 根据当前智能体显示专业建议问题 |

---

## 五、完整使用说明

### 5.1 环境要求

- **Node.js**：已安装（`node --version` 可验证）
- **QClaw**：已运行（Gateway 服务在线）
- **操作系统**：Windows 10/11（本项目在 Windows 下开发）

### 5.2 启动方式（推荐 PowerShell）

```powershell
# 进入项目目录
cd F:\Qclawjob

# 启动服务
.\start-server.ps1 start

# 其他命令
.\start-server.ps1 stop      # 停止服务
.\start-server.ps1 restart   # 重启服务
.\start-server.ps1 status    # 查看状态
```

### 5.3 备用启动方式

**Node.js Launcher（跨平台）**：
```bash
cd F:\Qclawjob
node launcher.js start    # 启动
node launcher.js stop     # 停止
node launcher.js status   # 状态
```

**直接启动（调试）**：
```bash
cd F:\Qclawjob\backend
node server.js
```

### 5.4 访问方式

启动成功后，打开浏览器访问：

```
http://localhost:3000
```

**局域网访问**：在同一网络内的其他设备上访问 `http://<服务器IP>:3000`

### 5.5 使用流程

1. **选择智能体**：点击顶部下拉菜单切换（🏗 企业工程管理总监 / 🏆 工程创优专家）
2. **输入消息**：在底部输入框输入问题，按 Enter 发送
3. **上传文件**：点击 📎 按钮选择文件（支持 txt/pdf/doc/xlsx/png 等）
4. **查看回复**：SSE 流式显示，逐字输出
5. **切换智能体**：下拉菜单选择其他智能体，历史记录自动切换

### 5.6 已配置智能体

| 智能体 ID | 名称 | 头像 | 人设来源 | 专业领域 |
|-----------|------|------|----------|----------|
| `agent-168feb0f` | 企业工程管理总监 | 🏗 | `workspace-agent-168feb0f/SOUL.md` | 施工企业管理、项目进度/安全/质量/成本 |
| `agent-2e63987e` | 工程创优专家 | 🏆 | `workspace-agent-2e63987e/SOUL.md` | 鲁班奖、创优方案策划、优质工程案例 |

---

## 六、关键配置

### 6.1 后端配置（`backend/server.js`）

```javascript
const GATEWAY_HOST = '127.0.0.1';      // QClaw Gateway 地址
const AUTH_TOKEN = 'ce90499e...';       // Gateway 认证 Token（从 openclaw.json 读取）
const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB 文件大小限制
const PORT = 3000;                      // 本服务端口
```

### 6.2 智能体注册

在 `server.js` 的 `AGENTS` 对象中添加：

```javascript
const AGENTS = {
    'agent-168feb0f': { name: '企业工程管理总监', avatar: '🏗' },
    'agent-2e63987e': { name: '工程创优专家', avatar: '🏆' },
    // 新增智能体：
    'agent-xxxxxxx': { name: '新智能体名称', avatar: '🎯' }
};
```

> 新增智能体需确保 `workspace-agent-xxx/SOUL.md` 存在，System Prompt 会自动读取。

---

## 七、故障排查

| 问题 | 原因 | 解决 |
|------|------|------|
| 端口 3000 被占用 | 旧进程未清理 | `start-server.ps1` 自动检测并清理 |
| Gateway 连接失败 | 端口未检测或已变化 | 检查 `openclaw gateway status`，server.js 每 30s 自动重检 |
| 文件上传无响应 | 文件过大或格式不支持 | 检查文件 ≤10MB，格式在允许列表中 |
| 中文乱码 | 批处理编码问题 | 使用 PowerShell 或 Node.js 启动，避免批处理 |
| 智能体未显示 | SOUL.md 不存在 | 检查 `workspace-agent-xxx/SOUL.md` 是否存在 |

---

## 八、注意事项

1. **不要直接双击 `index.html`**：前端依赖后端 API，必须通过 `http://localhost:3000` 访问
2. **PowerShell 中 `$pid` 是保留变量**：脚本中使用 `$serverPid` 避免冲突
3. **系统安全策略**：可能拦截 `.bat` 文件操作，优先使用 PowerShell 或 Node.js 启动
4. **文件上传即时清理**：上传文件处理后立即删除，如需保留请修改代码
5. **Gateway 端口动态变化**：QClaw 重启后端口可能变化，服务会自动重新检测

---

## 九、版本历史

| 日期 | 功能 | 说明 |
|------|------|------|
| 2026-06-12 | 初始搭建 | 创建 Express 反向代理，Gateway SSE 流式连接 |
| 2026-06-12 | SSE 流式修复 | 从 `http.request` 改为 `fetch + getReader()`，解决 socket hang up |
| 2026-06-12 | 文件上传 | 支持 multipart 上传、文本提取、即时清理 |
| 2026-06-12 | 网关端口修正 | 修正 Gateway 端口为动态检测（原硬编码 62433） |
| 2026-06-13 | 多智能体切换 | 支持 "企业工程管理总监" + "工程创优专家" 切换，System Prompt 联动 |
| 2026-06-13 | 启动脚本优化 | 新增 PowerShell 和 Node.js 启动器，支持 start/stop/restart/status |
| 2026-06-13 | 端口自动检测 | 三层检测（配置文件 → status 命令 → netstat），自动适应端口变化 |

---

*本文档基于实际项目文件生成，确保准确性与可复现性。*
