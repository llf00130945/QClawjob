# start.bat 修复总结

## 问题
用户报告：QClaw 重启后，双击 `start.bat`，后端服务没有启动。

## 根因分析
`start.bat` Step 3 中清理旧进程的逻辑有 bug：

```bat
for /f "tokens=5" %%a in ('netstat -ano ^| findstr ":3000" ^| findstr LISTENING') do (
```

`netstat -ano` 输出格式：
```
TCP    0.0.0.0:3000           0.0.0.0:0              LISTENING       14908
```

按空格分割后 `tokens=5` 取到的是 `0.0.0.0:3000`（地址），**不是 PID**！
导致 `taskkill /F /PID 0.0.0.0:3000` 执行失败，旧进程没有被杀死，
新服务器启动时端口冲突（`EADDRINUSE`），启动失败。

## 修复方案
改为遍历所有 token，取最后一个（即 PID）：

```bat
for /f "tokens=*" %%a in ('netstat -ano ^| findstr ":3000" ^| findstr LISTENING') do (
    set "line=%%a"
    for %%b in (%%a) do set "lastToken=%%b"
    if defined lastToken (
        echo [INFO] Killing old process PID:!lastToken!
        taskkill /F /PID !lastToken! >nul 2>&1
        timeout /t 1 /nobreak >nul
    )
)
```

同时确保 `setlocal EnableDelayedExpansion` 已启用，以支持 `!lastToken!` 变量扩展。

## 验证
- 旧进程 PID 14908 被正确检测并杀死 ✓
- 新服务器启动成功，端口 3000 正常监听 ✓
- `/api/status` 返回 `connected`，Gateway 端口 63360 ✓
- `/api/agents` 返回两个智能体列表 ✓

## 其他改进
- 使用 UTF-8 BOM 编码写入，避免中文乱码
- 添加 `server.log` 日志文件，启动失败时可查看错误信息
- 增加 `Step 0: Check Node.js`，提前发现环境缺失问题
